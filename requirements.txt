# QRUDO -- both halves install from this one file.
#
#   python3 -m venv .venv
#   .venv/bin/python -m pip install -r requirements.txt      (macOS/Linux)
#   .venv\Scripts\python -m pip install -r requirements.txt  (Windows)
#
# The pyobjc lines carry a marker so pip skips them on Windows, where they
# have no wheels and fail to build.

# --- Vision Engine ---------------------------------------------------------
absl-py==2.5.0
certifi==2026.7.22
cffi==2.1.1
contourpy==1.3.3
cycler==0.12.1
flatbuffers==25.12.19
fonttools==4.63.0
kiwisolver==1.5.0
matplotlib==3.11.1
mediapipe==1.0.0
numpy==2.4.6
opencv-contrib-python==5.0.0.93
opencv-python==5.0.0.93
packaging==26.3
pillow==12.3.0
pycparser==3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0

# --- Computer Control Engine ----------------------------------------------
# macOS only: media keys, seeking and global hotkeys. Volume and brightness
# work without them. Windows needs nothing extra -- it uses ctypes and
# PowerShell, both built in.
pyobjc-framework-Quartz>=10.0 ; sys_platform == "darwin"
pyobjc-framework-Cocoa>=10.0 ; sys_platform == "darwin"
